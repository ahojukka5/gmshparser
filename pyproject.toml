[project]
name = "gmshparser"
version = "0.3.1"
description = "A lightweight, dependency-free Python package for parsing ASCII Gmsh MSH files"
authors = [
    { name = "Jukka Aho", email = "ahojukka5@gmail.com" },
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
numpy = [
    "numpy>=1.26,<3",
]

[project.scripts]
gmshparser = "gmshparser.cli:main"

[project.urls]
Documentation = "https://ahojukka5.github.io/gmshparser/"
Issues = "https://github.com/ahojukka5/gmshparser/issues"
Repository = "https://github.com/ahojukka5/gmshparser"

[dependency-groups]
test = [
    "numpy>=1.26,<3",
    "pytest>=8,<10",
    "pytest-cov>=4,<8",
]
lint = [
    "ruff>=0.15,<1",
]
docs = [
    "mkdocs>=1.6,<2",
    "mkdocs-material>=9.5,<10",
    "mkdocstrings[python]>=0.26,<1.1",
    "numpy>=1.26,<3",
]
visualization = [
    "matplotlib>=3.5,<3.12",
]
dev = [
    { include-group = "test" },
    { include-group = "lint" },
]

[tool.uv]
default-groups = ["dev"]

[build-system]
requires = ["uv_build>=0.11.32,<0.12"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "gmshparser"
module-root = ""

[tool.pytest.ini_options]
minversion = "8.0"
addopts = "--cov=gmshparser -v --cov-report=term-missing"
testpaths = [
    "tests",
]

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = [
    "E4",
    "E7",
    "E9",
    "F",
    "B",
    "I",
    "UP",
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "auto"
